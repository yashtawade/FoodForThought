<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_comment_write"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.yashtawade.foodforthought.activities.CommentWriteActivity">

    <EditText
        android:id="@+id/comment_write_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:padding="5dp"
        android:gravity="top|left"
        android:inputType="textMultiLine"
        android:minLines="5"
        android:lines="5"
        android:maxLines="15"
        android:scrollbars="vertical"
        android:textCursorDrawable="@drawable/color_cursor"
        android:hint="Comment here..."
        android:background="@drawable/shape_edit_text_border"/>

    <Button
        android:id="@+id/comment_write_submit_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginRight="30dp"
        android:layout_marginLeft="30dp"
        android:textAllCaps="false"
        android:background="@drawable/shape_button_general"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:text="Submit"/>
</LinearLayout>
